---
const isHome = Astro.url.pathname === "/";

const links = [
    { href: "/projects/", label: "projects" },
    { href: "/articles/", label: "articles" },
    { href: "/about/", label: "about" }
];
---

<nav class={isHome ? "home" : "other"}>
    {!isHome && <a href="/">home</a>}
    {
        links.map(({ href, label }) => (
            <a href={href} class={Astro.url.pathname === href ? "active" : undefined}>
                {label}
            </a>
        ))
    }
</nav>

<style lang="scss">
    @use "styles/colors";

    nav {
        display: grid;
        gap: 1rem;
        padding: 1.25rem;
    }

    .home {
        grid-template-columns: repeat(3, min-content);
        justify-content: end;
    }

    .other {
        grid-template-columns: 1fr repeat(3, min-content);
    }

    a {
        justify-self: start;
        color: colors.$grey;
        text-decoration: none;
    }

    a:hover {
        color: white;
    }

    .active {
        color: white;
    }
</style>
