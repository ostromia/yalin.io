<div class="wrapper">
    <div class="eg">e.g.,</div>

    <div class="title">
        <slot name="title" />
    </div>

    <div class="body">
        <slot name="body" />
    </div>
</div>

<style>
    .wrapper {
        display: grid;
        grid-template-areas:
            "eg title"
            "body body";

        grid-template-columns: auto 1fr;

        gap: 0.5rem;

        border: 1px solid lightgrey;

        padding: 0.5rem;
    }

    .eg {
        grid-area: eg;
        display: flex;
        justify-content: center;
        align-items: center;

        color: lightgrey;
    }

    .title {
        grid-area: title;
        display: flex;
        align-items: center;
    }

    .body {
        grid-area: body;
    }
</style>
