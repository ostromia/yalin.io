---
import { Time } from "~components/elements";
import Footer from "~components/Footer.astro";
import Head from "~components/Head.astro";
import Navigation from "~components/Navigation.astro";
import "~styles/global.css";
import "~styles/markdown.css";
import "katex/dist/katex.min.css";

interface Props {
    title: string;
    description?: string;
    publicationDate: Date;
}

const { title, description, publicationDate } = Astro.props;
---

<html lang="en">
    <!-- TODO: scheme type set to optional string yet custom loader ensures description is present -->
    <Head title={title} description={description || ""} />

    <body>
        <Navigation />
        <main>
            <article>
                <header>
                    <Time date={publicationDate} format="D MMMM YYYY" />
                    <h1>{title}</h1>
                </header>

                <slot />
            </article>
        </main>

        <Footer />
    </body>
</html>

<style>
    header {
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 0.5rem;
    }

    h1 {
        margin: 0;
        text-align: center;
    }
</style>
